apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pgmicroservice-ingress
  namespace: dockeranto1          # change if your namespace is different
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  ingressClassName: nginx
  rules:
    - host: dockeranto1.57.158.184.247.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: pgmicroservice-svc
                port:
                  number: 80
